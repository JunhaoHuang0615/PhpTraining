通栏：自适应浏览器宽度的部分
版心：固定一个宽度，并且让容器居中

等高布局：
    即一个容器中的两个子元素所占的高度相同，且会根据“内容”自适应
    利用margin-bottom为负值，padding-bottom为正值，且大小相同，相互抵消来完成，父级overflow
    原理是其实子容器已经被padding撑开了，但由于margin为负值，又把父级缩回去了，因此此时子集被撑开，父级只能又子元素的内容撑开

三列布局：三列布局，左右固定，中间自适应
    1.bfc
    2.定位
    3.浮动（双飞翼布局，圣杯布局）
    4.flex弹性

    