1.选取指定区域
2.ctrl+c
3.ctrl+n 新建文件，且默认会是选区大小
4.ctrl+v
5.存储为web格式

利用切片工具，先切取，然后批量生成图片，file->export->存储为web格式，有切片的时候会自动生成图片

psd档案，根据图层自动切图
    编辑 -> 首选项 -> 生成器： 启动生成器
    文件-> 生成 -> 图像资源 打勾
    接着把要生成图片的图层改名： 如icon.png 则会自动生成图片了
    如果在生成的同时，想改变图片的大小，则命名为 50%    icon2-pad.png